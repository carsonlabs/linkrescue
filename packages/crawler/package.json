{
  "name": "@linkrescue/crawler",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'No build needed'",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@linkrescue/types": "workspace:*",
    "cheerio": "^1.0.0-rc.12",
    "fast-xml-parser": "^5.5.7"
  },
  "devDependencies": {
    "@linkrescue/config": "workspace:*",
    "@types/node": "^20.11.17",
    "typescript": "^5.3.3"
  }
}
